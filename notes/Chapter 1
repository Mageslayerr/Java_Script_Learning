// Anything following double slashes is a comment

//A Variable is a symbolic name for a Value.
//Variables are delcared with the keyword "let"
let x;              // delcare a variable named x.

//Values can be assigned to variables with a "=" sign
x=0;                //Now the variable x has the vlaue 0
x                   //=> 0: A variable evaluates to its value.

//Javascript supports several types of values

x = 1;                  // Numbers
x = 0.01;               // Numbers can be integers or reals.
x = "Hello World";      // Strings of of text in quotation marks.
x = 'JavaScript';       // Single quote marks do the same thing.
x = true;               // A boolean value.
x = false;              // Another boolean value.
x = null;               // Null means no value.
x = undefined;          // undefined is another special value like null.


//Javascript's most important data type is the object.
//An object is a collect of name/vlaue pairs, or a string to value map.
let book ={             // Objects are incloised in curly braces
    topic:"JavaScript"  // The property "topic" has value "JavaScript".
    edition: 7           // The property "edition" has value 7
};                      // The curly brace marks the end of the object.

//Access the properties of an object with "." or "[]":
book.topic              // => "JavaScript"
book["edition"]         // => 7: another way to access property values
book.author="Flanagan"; //Create a new property by assignment.
book.contents = {};     // {} is an empty obnject with no properties

// Conditionall access properties with "?".
book.contents?.ch01?.sect1 // => undefined: book.contents has no ch01 property.

//Javascript also supports arrays (numerically indexed lists) of values:
let primes =[2,3,5,7]; // An array of 4 values, delimited with [ and ].
primes[0]              // =>: the first element (index 0) of the array (0 is first)
primes.length          // => 4: how many elements in the array.
primes[primes.length-1] //=> 7: the last element of the array.
primes[4] = 9;          //Add a new element by assignment.
primes [4] = 11;        //Or alter an existing element by assignment.
let empty [];           //[] is an empty array with no elements.
empty.length            // => 0

//Arrays  and objects can hold other arrays and objects:
let points [            //An array with two elements.
    {x: 0, y: 0},       // Each element is an object.
    {x: 1, y: 1}
];
let data ={                // An object witrh two propertie4s
    trial1:[[1,2], [3,4]], // The value of each property is an array
    trial2: [[2,3],[4,5]]  // The elements of the arrayus are arrays
}:

//Operators act on values (the operands) toi produce a new value.
//Arithmetic operators are some of the simplest:
3+2;    // => 5: addition
3-2;    // => 1: subtraction
3*2;   // => 6: multiplication
3/2;   // => 1.5: division
3%2;   // => 1: remainder
points [1].x - points[0].x // => 1: subtraction of object properties
"3" + "2" // => "32": string concatenation

//Javascript defines some shorthand arithmetic operators:
let count = 0;              //Define a variable
count++;                    // Increment the variable
count--;                    // Devcrement the variable
count+= 2;                  // Add 2: same as the count = count + 2;
count *= 3;                 // Multiply by 3: same as count = count * 3;
count                       // => 6: variable names are expressions, too.

//Equality and relational operators test whether two values are equal.
// Unequal, less than, greater than, and so on. They evaluate to true or false.
let x =2,y=3;               // These = signs are assignment, not equality tests.
x===y                       // => false: x is not equal to y
x!==y                       // => true: x is not equal to y
x<y                         // => true: x is less than y
x<=y                        // => true: x is less than or equal to y
x>y                         // => false: x is not greater than y
x>=y                        // => false: x is not greater than or equal to y
"two"==="three"             // => false: two is not equal to three
"two">"three"               // => true: "tw" is alphabetically greater than "th"
false === (x>y)             // => true: false is equal to false

//logical operatoirs combine or invert boolean values:
(x===2) && (y===3)         // => true: both conditions are true -- "&&" is "and"
(x>3) || (y<3)                // => false: neither condition is true -- "||" is "or"
!(x===y)                     // => true: "!" inverts a bolean value -- "!" is "not"

