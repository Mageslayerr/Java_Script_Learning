//JavaScript suports more complex mathematical operations through a set of functions and constants defined as properties of the Math object:
Math.pow(2,53) // => 9007199254740992: 2 to the 53rd power
Math.round(0.6) // => 1: round to the nearest integer
Math.ceil(0.6) // => 1: round up to the next integer
Math.floor(0.6) // => 0: round down to the previous integer
Math.abs(-5) // => 5: absolute value
Math.max(3,5,1,-9) // => 5: maximum of the arguments
Math.min(3,5,1,-9) // => -9: minimum of the arguments
Math.random() // => 0.123456789: a random number between 0 and 1
Math.PI // => 3.141592653589793: the ratio of the circumference of a circle to its diameter
Math.E // => 2.718281828459045: the base of natural logarithms
Math.sqrt(3) // => 1.7320508075688772: the square root of 3
Math.pow(3,1/3) // => 1.4422495703074083: the cube root of 3
Math.sin(0) // Trigonometry: also math.cos, math.tan, math.asin, math.acos, math.atan, and math.atan2
Math.log(10) // => 2.302585092994046: natural logarithm of 10
Math.log(100)/Math.LN10 // => 2: base-10 logarithm of 100
Math.log(512)/Math.LN2 // => 9: base-2 logarithm of 512
Math.exp(3) // => 20.085536923187668: e to the 3rd power

//ES6 defines more functions on the Math Object:
Math.cbrt(27) // => 3: cube root of 27  
Math.hypot(3,4) // => 5: the hypotenuse of a right triangle with sides of length 3 and 4
Math.log10(100) // => 2: base-10 logarithm of 100
Math.log2(1024) // => 10: base-2 logarithm of 1024
Math.log1p(x) // => 0.6931471805599453: natural logarithm of 1+x
Math.expm1(x) // => 1.718281828459045: e^x - 1
Math.sign(x) // => 1: the sign of x, either -1, 0, or 1
Math.imul(2,3) // => 6: the result of a 32-bit integer multiplication of x and y
Math.clz32(0xf) // => 28: the number of leading zero bits in the 32-bit binary representation of x
Math.trunc(3.9) // => 3: the integer part of x, removing any fractional digits
Math.fround(3.5) // => 3.5: the nearest 32-bit single precision float representation of x
Math.sinh(x) // => 0.8686709614860095: the hyperbolic sine of x
Math.asinh(x) // => 0.881373587019543: the hyperbolic arcsine of x


Infinity // A posityive number that is too big to represent
Number.positive_infinity // Another way to represent positive infinity
1/0 // => Infinity: division by zero produces infinity
Number.Max_value *2 // => Infinity: multiplying a very large number by 2 produces infinity

-Infinity // A negative number that is too small to represent
-Number.negative_infinity // Another way to represent negative infinity
-(-1/0) // => -Infinity: division by zero produces negative infinity
-Number.Min_value *2 // => -Infinity: multiplying a very small number by 2 produces negative infinity

NaN // The not-a-number value
Number.NaN
0/0 // => NaN: division of zero by zero produces NaN
Infinity/Infinity // => NaN: division of infinity by infinity produces NaN

Number.Min_value/2 // => 0: underflow
-Number.Min_value/2 // => -0: negative zero
-1/Infinity // => -0: negative zero

let zero =0; //Regular zero
let negativeZero = -0; // Negative zero
zero === negz  // => true: negative zero is equal to regular zero
1/zero ===1/negz // => false: but they are not identical. 1/0 is positive infinity, while 1/-0 is negative infinity.

